/** Placeholder until auth UI lands in a later commit. */
export default function LoginPage() {
  return (
    <main style={{ padding: "2rem", fontFamily: "system-ui, sans-serif" }}>
      <h1>Eagle Bank</h1>
      <p>Login — coming in a later commit.</p>
    </main>
  );
}
